#include <stdio.h>

int main()
{

    char ch = "k";

    if (ch >= "A" && ch <= "Z")
    {
        printf(" %c is Uppercase ",ch);
    }
    else if (ch >= "a" && ch <= "z")
    {
        printf("%c is Lowercase ",ch);
    }

  
}